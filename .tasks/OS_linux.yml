---
version: '3'

tasks:

  install:
    desc: Install required tools for local developmen
    cmds:
    - sudo apt-get install -y yamllint jq shellcheck sshpass
    - sodu apt-get install nodejs npm
    - sudo pip3 install pre-commit
    - sudo pip3 install pip-upgrader
    - npm install -g npm-check-updates
