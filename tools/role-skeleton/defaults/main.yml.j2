---
# role: {{ role_name }}
# file: defaults/main.yml

# The role is disabled by default, so you do not get in trouble.
# Checked in tasks/main.yml which includes tasks.yml if enabled.
{{ role_name | replace('ansible_role_', '') }}_enabled: true
{{ role_name | replace('ansible_role_', '') }}_cache_seconds: 3600
{{ role_name | replace('ansible_role_', '') }}_packages: []
{{ blank_line }}
